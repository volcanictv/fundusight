Colab/Kaggle training notebooks live here. Keep one notebook per model
(e.g., `train_dr_efficientnet.ipynb`) and export trained weights to
Google Drive / Kaggle Datasets rather than committing them to git.
